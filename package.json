{
  "name": "preimage-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo home for @somnai-dreams/preimage and adjacent packages.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/somnai-dreams/preimage.git"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "tsc",
    "build:package": "bun --cwd packages/preimage run build",
    "build:layout-algebra": "bun --cwd packages/layout-algebra run build",
    "build:all": "bun run build:package && bun run build:layout-algebra",
    "build:demos": "rm -rf dist-demos && bun build pages/demos/*.html --outdir dist-demos --root pages/demos && bun build pages/bench/*.html --outdir dist-demos/bench --root pages/bench && cp -r pages/assets/. dist-demos/assets/",
    "start": "HOST=${HOST:-0.0.0.0} PORT=${PORT:-3000} bun --hot pages/server.ts",
    "start:windows": "bun --hot pages/server.ts"
  },
  "devDependencies": {
    "@chenglou/pretext": "^0.0.5",
    "@types/bun": "latest",
    "playwright": "^1.56.0",
    "typescript": "^5.6.0"
  }
}
